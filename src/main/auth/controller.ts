import { Request, Response } from "express"
import { TRegisterReq } from "./models"

export const register = async (req: Request<{},TRegisterReq>, res: Response) => {
    try {
        
    } catch (error) {
        
    }
}